@model ListaModel
@{
    ViewData["Title"] = "Criar Ubuntu";
}
<div class="text-center">

<h1 class= 'display-4'>Criar Ubuntu</h1>

</div>
<form asp-controller="Lista" asp-action="Criar" method="post">
  
  <div class="form-group">
    <label for="nome" class="form-label">Nome</label>
    <input type="text" asp-for="Nome" class="form-control" placeholder = "Digite seu nome" maxlength="100" minlength="3">
  
  </div>

  <div class="form-group">
    <label for="nome" class="form-label">Linguagem</label>
    <input type="text" asp-for="Linguagem" class="form-control" placeholder = "Linguagem que conhece" maxlength="100" minlength="2">

  </div>

  <div class="form-group">
    <label for="nome" class="form-label">Conhecimento</label>
    <input type="number" asp-for="Conhecimento" class="form-control" placeholder = "Insira do 0 ao 10" maxlength="100" minlength="0">

  </div>
        
        <script>
            function funcao1()
            {
            alert("Ubuntu Adicionado com Sucesso !");
            }
        </script>

        <button type="submit" class="btn btn-primary" onclick="funcao1()" value="Exibir Alert">Adicionar</button>
        <a class="btn btn-secondary" asp-controller="Lista" asp-action="Index" role= "button">Voltar</a>
</form>

